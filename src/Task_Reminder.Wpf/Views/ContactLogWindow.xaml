<Window x:Class="Task_Reminder.Wpf.Views.ContactLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Contact Log"
        Height="320"
        Width="420"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Contact Type" VerticalAlignment="Center" />
        <ComboBox Grid.Column="1" Margin="8,0,0,12" ItemsSource="{Binding ContactTypes}" SelectedItem="{Binding ContactType}" />
        <TextBlock Grid.Row="1" Text="Outcome" VerticalAlignment="Center" />
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="8,0,0,12" ItemsSource="{Binding ContactOutcomes}" SelectedItem="{Binding Outcome}" />
        <TextBlock Grid.Row="2" Text="Notes" VerticalAlignment="Top" />
        <TextBox Grid.Row="2" Grid.Column="1" Margin="8,0,0,12" AcceptsReturn="True" TextWrapping="Wrap" Text="{Binding Notes}" />
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Margin="0,0,8,0" Content="Cancel" Click="Cancel_Click" />
            <Button Width="90" Content="Save" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
        </StackPanel>
    </Grid>
</Window>
