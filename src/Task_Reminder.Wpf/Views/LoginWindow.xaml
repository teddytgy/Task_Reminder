<Window x:Class="Task_Reminder.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Task Reminder Login"
        Height="240"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="24" FontWeight="SemiBold" Text="Dental Front Desk Task Reminder" />
        <TextBlock Grid.Row="1" Margin="0,8,0,16" Foreground="#555" Text="{Binding StatusMessage}" />

        <ComboBox Grid.Row="2"
                  Height="36"
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  DisplayMemberPath="DisplayName" />

        <Button Grid.Row="3"
                Width="120"
                Height="36"
                HorizontalAlignment="Right"
                Margin="0,16,0,0"
                Content="Continue"
                Command="{Binding ContinueCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
    </Grid>
</Window>
